# Sensitive config (contains API tokens — never commit)
vaulted_config.json

# Pipeline scripts (local only)
vaulted_pipeline.py
vaulted_shorts.py
vaulted_batch.sh
vaulted_setup.sh
create_shortcut.sh
setup_cron.sh

# Runtime data
.vaulted_history.json
.vaulted_names.json
vaulted_episode_log.json

# Python cache
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
**/.DS_Store

# Audio/Video (large files — use GitHub Releases instead)
Audio/
Video/
Recordings/
Music/

# Logs
Logs/

# Artwork source files (cover.png in artwork/ is the one that matters)
VAULTED_Podcast_Artwork*.jpeg
VAULTED_Podcast_Artwork*.jpg
VAULTED_Podcast_Artwork*.png
Artwork/

# Git rewrite artifacts
.git-rewrite/

# Scripts (local only)
Scripts/
